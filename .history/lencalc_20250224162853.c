#include <stdio.h>
#include "mystrlen.c"

int main(void) {
   char s[MAXLEN];
   int len;

   // your code here to repeatedly prompt the user for a string 
   // and print its length using mystrlen

   return 0;
}